{
  "name": "audio-to-ableton",
  "version": "0.1.0",
  "private": true,
  "description": "Drop audio. Get an Ableton project. Free funnel for Ableton V1.",
  "type": "module",
  "scripts": {
    "predev": "node scripts/sync-model.mjs",
    "prebuild": "node scripts/sync-model.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync-model": "node scripts/sync-model.mjs"
  },
  "dependencies": {
    "@spotify/basic-pitch": "^1.0.1",
    "@tonejs/midi": "^2.0.28"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
