# TODO — audio-to-ableton

## Blocking for public launch

- [x] ~~**`.als` template (real schema).**~~ Shipped. Templates live in
      `public/als-template.xml` + `public/als-clip-template.xml`, generated by
      `scripts/build-als-template.mjs` from a captured Live 12.2 baseline.
      `src/als.ts` substitutes placeholders, gzips via `CompressionStream`.
      Smoke-tested: `node scripts/test-als.mjs` produces an .als that opens in
      Live 12. Schema target: Live 12.0+. Will need re-capture if Ableton bumps
      the schema (`MajorVersion="5"` + `MinorVersion`).
- [ ] **Ableton V1 download URL.** `src/main.ts` has `pollinations.ai/ableton-v1/download`
      hard-coded. Replace with the actual hosted URL once we have one.
- [ ] **Email capture for non-Mac users.** Today the CTA points at a 404. We need
      either a Pollinations-side `/notify` endpoint or a Mailchimp/ConvertKit form.
- [x] ~~**Bundle/host the Basic Pitch model locally.**~~ Shipped. Model files
      live in `public/model/` (~900KB total: `model.json` + one shard binary).
      `MODEL_URL` points at `/model/model.json`. `scripts/sync-model.mjs` keeps
      it in sync with `node_modules/@spotify/basic-pitch/model/` and is wired
      to `predev` / `prebuild` so a `npm install` bump auto-refreshes them.

## Quality / polish (pre-launch)

- [ ] **Mobile drop zone.** The `<input type=file>` will work but the experience
      is lacklustre on small screens. Add a friendlier "Tap to pick" treatment.
- [ ] **Personalised demo loop on the result page.** The plan calls for a 6-second
      video of V1 playing the user's notes. Punt to v0.2 — needs server-side
      rendering. For v0.1 a static loop is fine.
- [ ] **Save-and-resume handoff.** Generate a one-time link the user can paste
      into V1 after install, so the transcription is waiting for them. Server
      side: tiny KV with 24h TTL (Upstash free tier).

## Known limits (don't fix in v0.1)

- **Real-time playback decoder** for video files in `src/decode.ts` runs at
  whatever `playbackRate` the browser allows (capped 2–4×). For a 60s video this
  takes 15–30s. Acceptable for v0.1; replace with `ffmpeg.wasm` if it becomes a
  problem.
- **Stem separation** is not in scope. Suno-specific funnel waits for that.

## Voice / pitch tweaks

- Test alternative headlines (see `COPY.md` "Open questions").
- Decide on Spotify name-drop in the trust strip.
